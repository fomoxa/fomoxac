// GENERATED BY fomoxac
// DO NOT EDIT MANUALLY
// source: src/models/models.hpp
// model: DeviceState
// codec: unity
// fingerprint: sha256:4811419f2da07dfe59905a8375e58d32885a1c229ac202f3f621f1b45175b3ae
// fomoxac-version: 0.2.2
// generated-at: 2026-09-23T15:58:02Z

#pragma once

#include <cstddef>
#include <cstdint>
#include <string>
#include <vector>

#include "runtime.hpp"
#include "src/models/models.hpp"

namespace generated {

/// The "unity" codec for DeviceState, generated from its Fomoxa markers.
///
/// The wire layout, in declaration order (RFC-0002 §5.1):
///
///  0. `Id`: `u32`
///  1. `DisplayName`: `string`
struct DeviceStateUnityCodec {
    /// This message's name: `Model.codec`.
    static constexpr const char* kMessageName = "DeviceState.unity";

    /// This message's stable id, derived from its name alone.
    static constexpr std::uint32_t kMessageId = 0x9AF41CDEu;

    /// This message's wire-contract fingerprint - the same value
    /// handshake.hpp publishes, and the one a peer compares against.
    static constexpr std::uint64_t kFingerprint = 0x4811419F2DA07DFEULL;

    /// Writes the "unity" fields of `value`, in declaration order.
    static void encode(Writer& writer, const ::models::DeviceState& value) {
        writer.write_u32(value.Id);
        writer.write_string(value.DisplayName);
    }

    /// Reads the "unity" fields into `value`, in declaration order.
    ///
    /// Fields this codec does not carry are left as they were, which is what lets one
    /// model be split across several codecs.
    ///
    /// A field the stream ended before takes its zero value (RFC-0002 §9.1); a field
    /// the stream ended inside is an error. Bytes left over after the last field
    /// belong to a newer writer's model and are ignored.
    static DecodeError decode(Reader& reader, ::models::DeviceState& value) {
        if (reader.field_absent()) {
            value.Id = 0;
        } else {
            if (DecodeError error = reader.read_u32(value.Id); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.DisplayName = std::string();
        } else {
            if (DecodeError error = reader.read_string(value.DisplayName); !error.ok()) return error;
        }
        return DecodeError{};
    }
};

}  // namespace generated
