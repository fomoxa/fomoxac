// GENERATED BY fomoxac
// DO NOT EDIT MANUALLY
// source: src/models/models.hpp
// model: DeviceState
// codec: edge
// fingerprint: sha256:4625cb8e64b0827a10cda93e581ed653e550290885f97a5f857ec0ef39012b91
// fomoxac-version: 0.2.1
// generated-at: 2026-09-23T02:40:29Z

#pragma once

#include <cstddef>
#include <cstdint>
#include <string>
#include <vector>

#include "runtime.hpp"
#include "src/models/models.hpp"

namespace generated {

/// The "edge" codec for DeviceState, generated from its Fomoxa markers.
///
/// The wire layout, in declaration order (RFC-0002 §5.1):
///
///  0. `Id`: `u32`
///  1. `Temperature`: `f32`
struct DeviceStateEdgeCodec {
    /// This message's name: `Model.codec`.
    static constexpr const char* kMessageName = "DeviceState.edge";

    /// This message's stable id, derived from its name alone.
    static constexpr std::uint32_t kMessageId = 0x2796F8ACu;

    /// This message's wire-contract fingerprint - the same value
    /// handshake.hpp publishes, and the one a peer compares against.
    static constexpr std::uint64_t kFingerprint = 0x4625CB8E64B0827AULL;

    /// Writes the "edge" fields of `value`, in declaration order.
    static void encode(Writer& writer, const ::models::DeviceState& value) {
        writer.write_u32(value.Id);
        writer.write_f32(value.Temperature);
    }

    /// Reads the "edge" fields into `value`, in declaration order.
    ///
    /// Fields this codec does not carry are left as they were, which is what lets one
    /// model be split across several codecs.
    ///
    /// A field the stream ended before takes its zero value (RFC-0002 §9.1); a field
    /// the stream ended inside is an error. Bytes left over after the last field
    /// belong to a newer writer's model and are ignored.
    static DecodeError decode(Reader& reader, ::models::DeviceState& value) {
        if (reader.field_absent()) {
            value.Id = 0;
        } else {
            if (DecodeError error = reader.read_u32(value.Id); !error.ok()) return error;
        }
        if (reader.field_absent()) {
            value.Temperature = 0.0f;
        } else {
            if (DecodeError error = reader.read_f32(value.Temperature); !error.ok()) return error;
        }
        return DecodeError{};
    }
};

}  // namespace generated
