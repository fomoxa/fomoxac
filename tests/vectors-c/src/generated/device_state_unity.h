// GENERATED BY fomoxac
// DO NOT EDIT MANUALLY
// source: src/models/models.h
// model: DeviceState
// codec: unity
// fingerprint: sha256:4811419f2da07dfe59905a8375e58d32885a1c229ac202f3f621f1b45175b3ae
// fomoxac-version: 0.2.1
// generated-at: 2026-09-23T09:13:20Z

#pragma once

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>

#include "runtime.h"
#include "src/models/models.h"

// The "unity" codec for DeviceState, generated from its Fomoxa markers.
//
// The wire layout, in declaration order (RFC-0002 SS5.1):
//
//  0. `Id`: `u32`
//  1. `DisplayName`: `string`

static const char *const DeviceStateUnityCodec_MESSAGE_NAME = "DeviceState.unity";
static const uint32_t DeviceStateUnityCodec_MESSAGE_ID = 0x9AF41CDEu;
static const uint64_t DeviceStateUnityCodec_FINGERPRINT = 0x4811419F2DA07DFEULL;

// Writes the "unity" fields of `*value`, in declaration order. Returns `false` (buffer
// untouched beyond what was already written) if growing the writer's buffer failed.
static inline bool DeviceStateUnityCodec_encode(FomoxaWriter *writer, const struct DeviceState *value) {
    if (!fomoxa_writer_write_u32(writer, value->Id)) return false;
    if (!fomoxa_writer_write_string(writer, value->DisplayName)) return false;
    return true;
}

// Reads the "unity" fields into `*value`, in declaration order.
//
// Fields this codec does not carry are left as they were, which is what lets one
// model be split across several codecs.
//
// A field the stream ended before takes its zero value (RFC-0002 SS9.1); a field
// the stream ended inside is an error. Bytes left over after the last field
// belong to a newer writer's model and are ignored.
//
// `*value` must be zero-initialized, freed, or filled by an earlier decode,
// whose buffers this one reuses.
static inline FomoxaDecodeError DeviceStateUnityCodec_decode(FomoxaReader *reader, struct DeviceState *value) {
    if (fomoxa_reader_field_absent(reader)) {
        value->Id = 0;
    } else {
        FomoxaDecodeError error = fomoxa_reader_read_u32(reader, &value->Id);
        if (!fomoxa_decode_error_ok(&error)) return error;
    }
    if (fomoxa_reader_field_absent(reader)) {
        free((void *)value->DisplayName);
        value->DisplayName = NULL;
    } else {
        FomoxaDecodeError error = fomoxa_reader_read_string_into(reader, &value->DisplayName);
        if (!fomoxa_decode_error_ok(&error)) return error;
    }
    return fomoxa_decode_ok();
}
