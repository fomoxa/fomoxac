// GENERATED BY fomoxac
// DO NOT EDIT MANUALLY
// source: src/models/player.h
// model: Player
// codec: edge
// fingerprint: sha256:01fc1f8d5768cded1cb94f6d37b2f32412b40376e78e3c36ac8a5e70cee183f4
// fomoxac-version: 0.2.2
// generated-at: 2026-09-23T15:58:02Z

#pragma once

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>

#include "runtime.h"
#include "src/models/player.h"

// The "edge" codec for Player, generated from its Fomoxa markers.
//
// The wire layout, in declaration order (RFC-0002 SS5.1):
//
//  0. `Id`: `u32`
//  1. `X`: `f32`
//  2. `Y`: `f32`
//  3. `Name`: `string`
//  4. `Payload`: `bytes`

static const char *const PlayerEdgeCodec_MESSAGE_NAME = "Player.edge";
static const uint32_t PlayerEdgeCodec_MESSAGE_ID = 0x5AD3FC4Fu;
static const uint64_t PlayerEdgeCodec_FINGERPRINT = 0x01FC1F8D5768CDEDULL;

// Writes the "edge" fields of `*value`, in declaration order. Returns `false` (buffer
// untouched beyond what was already written) if growing the writer's buffer failed.
static inline bool PlayerEdgeCodec_encode(FomoxaWriter *writer, const struct Player *value) {
    if (!fomoxa_writer_write_u32(writer, value->Id)) return false;
    if (!fomoxa_writer_write_f32(writer, value->X)) return false;
    if (!fomoxa_writer_write_f32(writer, value->Y)) return false;
    if (!fomoxa_writer_write_string(writer, value->Name)) return false;
    if (!fomoxa_writer_write_bytes(writer, &value->Payload)) return false;
    return true;
}

// Reads the "edge" fields into `*value`, in declaration order.
//
// Fields this codec does not carry are left as they were, which is what lets one
// model be split across several codecs.
//
// A field the stream ended before takes its zero value (RFC-0002 SS9.1); a field
// the stream ended inside is an error. Bytes left over after the last field
// belong to a newer writer's model and are ignored.
//
// `*value` must be zero-initialized, freed, or filled by an earlier decode,
// whose buffers this one reuses.
static inline FomoxaDecodeError PlayerEdgeCodec_decode(FomoxaReader *reader, struct Player *value) {
    if (fomoxa_reader_field_absent(reader)) {
        value->Id = 0;
    } else {
        FomoxaDecodeError error = fomoxa_reader_read_u32(reader, &value->Id);
        if (!fomoxa_decode_error_ok(&error)) return error;
    }
    if (fomoxa_reader_field_absent(reader)) {
        value->X = 0.0f;
    } else {
        FomoxaDecodeError error = fomoxa_reader_read_f32(reader, &value->X);
        if (!fomoxa_decode_error_ok(&error)) return error;
    }
    if (fomoxa_reader_field_absent(reader)) {
        value->Y = 0.0f;
    } else {
        FomoxaDecodeError error = fomoxa_reader_read_f32(reader, &value->Y);
        if (!fomoxa_decode_error_ok(&error)) return error;
    }
    if (fomoxa_reader_field_absent(reader)) {
        free((void *)value->Name);
        value->Name = NULL;
    } else {
        FomoxaDecodeError error = fomoxa_reader_read_string_into(reader, &value->Name);
        if (!fomoxa_decode_error_ok(&error)) return error;
    }
    if (fomoxa_reader_field_absent(reader)) {
        fomoxa_bytes_free(&value->Payload);
    } else {
        FomoxaDecodeError error = fomoxa_reader_read_bytes_into(reader, &value->Payload);
        if (!fomoxa_decode_error_ok(&error)) return error;
    }
    return fomoxa_decode_ok();
}
