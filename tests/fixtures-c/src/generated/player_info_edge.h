// GENERATED BY fomoxac
// DO NOT EDIT MANUALLY
// source: src/models/player.h
// model: PlayerInfo
// codec: edge
// fingerprint: sha256:b87a1c88c5b6e0ddeff880c1b11d61633d4c0cfc7fcd31a10bbea40d6c3fbbcd
// fomoxac-version: 0.2.1
// generated-at: 2026-09-23T09:13:20Z

#pragma once

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>

#include "runtime.h"
#include "src/models/player.h"

// The "edge" codec for PlayerInfo, generated from its Fomoxa markers.
//
// The wire layout, in declaration order (RFC-0002 SS5.1):
//
//  0. `Level`: `u32`

static const char *const PlayerInfoEdgeCodec_MESSAGE_NAME = "PlayerInfo.edge";
static const uint32_t PlayerInfoEdgeCodec_MESSAGE_ID = 0xC396A399u;
static const uint64_t PlayerInfoEdgeCodec_FINGERPRINT = 0xB87A1C88C5B6E0DDULL;

// Writes the "edge" fields of `*value`, in declaration order. Returns `false` (buffer
// untouched beyond what was already written) if growing the writer's buffer failed.
static inline bool PlayerInfoEdgeCodec_encode(FomoxaWriter *writer, const struct PlayerInfo *value) {
    if (!fomoxa_writer_write_u32(writer, value->Level)) return false;
    return true;
}

// Reads the "edge" fields into `*value`, in declaration order.
//
// Fields this codec does not carry are left as they were, which is what lets one
// model be split across several codecs.
//
// A field the stream ended before takes its zero value (RFC-0002 SS9.1); a field
// the stream ended inside is an error. Bytes left over after the last field
// belong to a newer writer's model and are ignored.
//
// `*value` must be zero-initialized, freed, or filled by an earlier decode,
// whose buffers this one reuses.
static inline FomoxaDecodeError PlayerInfoEdgeCodec_decode(FomoxaReader *reader, struct PlayerInfo *value) {
    if (fomoxa_reader_field_absent(reader)) {
        value->Level = 0;
    } else {
        FomoxaDecodeError error = fomoxa_reader_read_u32(reader, &value->Level);
        if (!fomoxa_decode_error_ok(&error)) return error;
    }
    return fomoxa_decode_ok();
}
