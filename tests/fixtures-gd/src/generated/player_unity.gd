# GENERATED BY fomoxac
# DO NOT EDIT MANUALLY
# source: src/models/player.gd
# model: Player
# codec: unity
# fingerprint: sha256:3a09927a6d1a81f17c7ca42ee0bd208ad552c9b688495fa42483e310b09381c6
# fomoxac-version: 0.2.1
# generated-at: 2026-09-21T14:58:50Z

class_name PlayerUnityCodec

# PlayerUnityCodec is the "unity" codec for Player, generated from its Fomoxa directives.
#
# The wire layout, in declaration order (RFC-0002 §5.1):
#
#  0. `id`: `u32`

# This message's name: Model.codec.
const MESSAGE_NAME := "Player.unity"

# This message's stable id, derived from its name alone.
const MESSAGE_ID: int = 0x48AE5CB2

# This message's wire-contract fingerprint - the same value handshake.gd publishes,
# and the one a peer compares against.
const FINGERPRINT: int = (0x3A09927A << 32) | 0x6D1A81F1

# Writes the "unity" fields of value, in declaration order.
static func encode(writer: FomoxaRuntime.Writer, value: Player) -> void:
	writer.write_u32(value.id)

# Reads the "unity" fields into value, in declaration order.
#
# Fields this codec does not carry are left as they were, which is what lets one
# model be split across several codecs.
#
# A field the stream ended before takes its zero value (RFC-0002 §9.1); a field
# the stream ended inside is an error. Bytes left over after the last field
# belong to a newer writer's model and are ignored.
static func decode(reader: FomoxaRuntime.Reader, value: Player) -> FomoxaRuntime.DecodeError:
	if reader.field_absent():
		value.id = 0
	else:
		var id_result = reader.read_u32()
		if id_result[1] != null:
			return id_result[1]
		value.id = id_result[0]
	return null
