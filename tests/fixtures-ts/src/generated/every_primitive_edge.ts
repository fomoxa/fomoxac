// GENERATED BY fomoxac
// DO NOT EDIT MANUALLY
// source: src/models/every_primitive.ts
// model: EveryPrimitive
// codec: edge
// fingerprint: sha256:2ec0c98895e5a4e1eb31bbd973766c1907415b625e8fb0228fc4d843535a6512
// fomoxac-version: 0.2.1
// generated-at: 2026-09-23T08:57:21Z

import { Writer, Reader } from "./runtime";
import { EveryPrimitive } from "../models/every_primitive";

/**
 * The `edge` codec for {@link EveryPrimitive}, generated from its Fomoxa attributes.
 *
 * The wire layout, in declaration order (RFC-0002 §5.1):
 *
 *  0. `Flag`: `bool`
 *  1. `Tiny`: `i8`
 *  2. `Byte`: `u8`
 *  3. `Small`: `i16`
 *  4. `Port`: `u16`
 *  5. `Offset`: `i32`
 *  6. `Count`: `u32`
 *  7. `Delta`: `i64`
 *  8. `Sequence`: `u64`
 *  9. `Ratio`: `f32`
 *  10. `Precise`: `f64`
 *  11. `Label`: `string`
 *  12. `Blob`: `bytes`
 */
export class EveryPrimitiveEdgeCodec {
    /** This message's name: `Model.codec`. */
    static readonly MESSAGE_NAME: string = "EveryPrimitive.edge";

    /** This message's stable id, derived from its name alone. */
    static readonly MESSAGE_ID: number = 0x19FCABFF;

    /**
     * This message's wire-contract fingerprint - the same value
     * `handshake.ts` publishes, and the one a peer compares against.
     */
    static readonly FINGERPRINT: bigint = 0x2EC0C98895E5A4E1n;

    /** Writes the `edge` fields of `value`, in declaration order. */
    static encode(writer: Writer, value: EveryPrimitive): void {
        writer.writeBool(value.Flag);
        writer.writeI8(value.Tiny);
        writer.writeU8(value.Byte);
        writer.writeI16(value.Small);
        writer.writeU16(value.Port);
        writer.writeI32(value.Offset);
        writer.writeU32(value.Count);
        writer.writeI64(value.Delta);
        writer.writeU64(value.Sequence);
        writer.writeF32(value.Ratio);
        writer.writeF64(value.Precise);
        writer.writeString(value.Label);
        writer.writeBytes(value.Blob);
    }

    /**
     * Reads the `edge` fields into `value`, in declaration order.
     *
     * Fields this codec does not carry are left as they were, which is what lets
     * one model be split across several codecs.
     *
     * A field the stream ended before takes its zero value (RFC-0002 §9.1); a field
     * the stream ended inside throws. Bytes left over after the last field belong to
     * a newer writer's model and are ignored.
     */
    static decode(reader: Reader, value: EveryPrimitive): void {
        value.Flag = reader.fieldAbsent() ? false : reader.readBool();
        value.Tiny = reader.fieldAbsent() ? 0 : reader.readI8();
        value.Byte = reader.fieldAbsent() ? 0 : reader.readU8();
        value.Small = reader.fieldAbsent() ? 0 : reader.readI16();
        value.Port = reader.fieldAbsent() ? 0 : reader.readU16();
        value.Offset = reader.fieldAbsent() ? 0 : reader.readI32();
        value.Count = reader.fieldAbsent() ? 0 : reader.readU32();
        value.Delta = reader.fieldAbsent() ? 0n : reader.readI64();
        value.Sequence = reader.fieldAbsent() ? 0n : reader.readU64();
        value.Ratio = reader.fieldAbsent() ? 0 : reader.readF32();
        value.Precise = reader.fieldAbsent() ? 0 : reader.readF64();
        value.Label = reader.fieldAbsent() ? "" : reader.readString(value.Label);
        value.Blob = reader.fieldAbsent() ? new Uint8Array(0) : reader.readBytes(value.Blob);
    }
}
